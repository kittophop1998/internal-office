services:
  frontend:
    image: internal-office:latest
    container_name: internal-office
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
